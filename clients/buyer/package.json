{
  "name": "drift-oracle-buyer-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Buyer-side smoke test for the Course Drift Oracle MCP server.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@x402/core": "^2.24.0",
    "@x402/evm": "^2.24.0",
    "viem": "^2.21.0"
  }
}
